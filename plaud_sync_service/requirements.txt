# Azure Container Apps Jobs - No HTTP server needed!

# Shared QuickScribe library (installed via editable install)
# Install with: pip install -e ../shared_quickscribe_py

# Azure SDK components
azure-cosmos>=4.5.0
azure-storage-blob>=12.19.0
azure-storage-queue>=12.8.0
azure-identity>=1.15.0

# Audio processing
ffmpeg-python>=0.2.0

# HTTP requests (for API calls, not serving)
requests>=2.31.0
aiohttp>=3.9.0

# Azure Speech Services client (installed via Dockerfile, not here)
# -e ./azure_speech/python-client

# Configuration and data formats
PyYAML>=6.0

# Logging and monitoring
opencensus-ext-azure>=1.1.9

# Speaker identification (torch/torchaudio/speechbrain installed separately in Dockerfile for CPU-only wheels)
numpy>=1.24.0
