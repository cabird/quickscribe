# Validation UI Server Requirements
# Install with: pip install -r requirements.txt

flask>=3.0.0
flask-cors>=4.0.0

# Note: The diarization experiment dependencies (data_loader, speaker_identifier, etc.)
# should already be installed in your environment
