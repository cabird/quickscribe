# QuickScribe TODOs

## 🔥 High Priority

### 1. Fix drag and drop file upload functionality
**Issue**: The drag and drop of an audio file doesn't work

**Implementation Plan**:
1. Fix MIME type configuration in `frontend_new/src/components/Layout/UploadTab.tsx`
   - Change `'audio/mp3'` to `'audio/mpeg'` 
   - Add support for additional formats (`audio/wav`, `audio/ogg`, `audio/opus`)
2. Verify authentication headers are included in upload requests
3. Update API URL to use environment variable instead of hardcoded path
4. Test upload functionality with various file formats
5. Add proper error handling for failed uploads

### 2. Implement systematic backend testing approach
**Issue**: Currently using a bunch of scripts that don't get run anywhere near as often as they should

**Implementation Plan**:
1. Set up pytest framework in backend directory
   - Create `tests/` directory structure
   - Add `pytest.ini` configuration file
   - Install pytest dependencies in requirements.txt
2. Create test categories:
   - Unit tests for database handlers
   - Integration tests for API endpoints
   - End-to-end tests for complete workflows
3. Set up test database configuration (separate from production)
4. Create test fixtures and utilities
5. Add CI/CD pipeline integration to run tests automatically
6. Migrate existing test scripts to proper test framework
7. Add test coverage reporting

## 📋 Medium Priority

### 3. Refactor test scripts to use db_handlers
**Issue**: Test scripts should use the db_handlers where possible instead of direct database access

**Implementation Plan**:
1. Audit all scripts in `/scripts/` directory
2. Identify scripts doing direct database operations
3. Refactor scripts to use handler factory pattern:
   - Import from `db_handlers.handler_factory`
   - Use `get_user_handler()`, `get_recording_handler()`, `get_transcription_handler()`
   - Replace direct Cosmos DB calls with handler methods
4. Update script documentation and usage examples
5. Test all refactored scripts to ensure functionality is preserved

### 4. Create recording deletion endpoint
**Issue**: Need an endpoint that deletes a recording record

**Implementation Plan**:
1. Verify if DELETE endpoint already exists in `backend/routes/api.py`
2. If missing, implement DELETE `/api/recordings/{recording_id}` endpoint:
   - Add authentication check
   - Verify user owns the recording
   - Delete associated transcriptions
   - Delete blob storage files
   - Remove recording from database
   - Return appropriate status codes
3. Add error handling for:
   - Recording not found
   - Unauthorized access
   - Blob deletion failures
4. Update frontend to use the deletion endpoint
5. Add tests for the deletion functionality


# Unprocessed Todos:

These are todos that I've added and haven't been planned out or examined by AI yet...

  * fix the notifications in the frontend.  The mantine notifications are weird and feel out of place.
  * the "view full" button in the recording workspace does nothing right now.  It should show the transript in the full window and blur everything else.
  X The layout of the workspace is weird.  We should have the transcript across the whole thing and operations below that.
  X try moving to a glassmorphic UI
  X try using fluent icons
  * user should be able to set the bearer token.  bonus points if we explain how to get it by using the dev tools in chrome when going to the plaud.ai site.
  * UI for showing rationale for speaker assignment
  * UI for setting the speaker names (get a speaker profile to help the user)
  * maintain a catalog of speakers profiles across all recordings so we can a) find recordings where they talk. b) can label them better in future recordings
  * check that type definitions for messages and responses are all in shared... I don't think they all are.
  * add auto-tagging
  * implement easy AI analysis of: summary, action items, keywords, Q&A
  * longer term, think of coaching on conversations
  * Allow to paste in transcripts and .vtt files of teams messages.
  * Allow the user to query an LLM with the transcript injected into the prompt.  This could be a full chat experience... need to think about the best experience for this...
  * organize the backend api more.  tags should have their own file.
  * implement handling of a dead letter queue or poison pill messages in the queue
  * If I paste in a transcript, check that it doesn't already exist as a transcript of a recording... not sure how to do this..
  * code review the /api/admin routes.  It''s like super verbose.  I wonder if there is a better way.  Gemini could probably refactor it?

